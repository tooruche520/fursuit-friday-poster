# FursuitFriday 發文神器 - 專案需求與功能規格文件 (PRD)

## 1. 基礎需求概述 (Core Requirements)

本專案旨在解決毛毛玩家（Fursuiter）跨平台發文時，需要反覆修改標記帳號與排版的痛點。
最核心的基礎需求如下：

* **AI 內文生成**：使用者上傳圖片後，系統能透過 AI 自動撰寫符合情境的貼文草稿。
* **多平台帳號綁定**：可以記錄同一位攝影師、毛裝工作室或搭檔在不同平台（X, IG, Bluesky 等）的不同帳號名稱。
* **自訂格式與自動群組化**：提供內文、標記者、Hashtag 的排版設定，並能自動將相同「角色定位」的標記者合併在同一行。
* **一鍵切換與複製**：在發文前選擇目標平台，系統會自動將所有標記替換成該平台的對應帳號，並提供一鍵複製完整排版內容的功能。

---

## 2. 系統架構與介面設計

* **前端框架**：Nuxt4 + shadcn-vue(tailwind css)。
* **UI 系統**：參考 `shadcn-vue` (或 `shadcn-ui`) 的設計語彙，強調無框線輸入風格、充足留白、圓角 (Radius) 以及高質感的按鈕回饋。
* **頁面路由 (SPA)**：單頁應用程式架構，包含頂部全域導覽列，可在兩個主要視窗間無縫切換：
1. **編輯貼文 (Generator View)**
2. **通訊錄管理 (Contacts View)**



---

## 3. 詳細功能規格

### 3.1 編輯貼文 (Generator View)

分為左側「操作區」與右側「預覽與輸出區（Sticky 吸頂）」。

#### A. AI 內文生成區

* **圖片上傳 UI**：提供點擊或拖曳上傳的虛線框介面。
* **AI 生成按鈕**：點擊後呼叫 AI API 生成文字。需有 Loading 狀態（如 Pulse 動畫），且在未上傳圖片時按鈕應為禁用 (Disabled) 狀態。
* **草稿編輯區**：提供 Textarea 讓使用者可以自由修改 AI 生成的內文或手動輸入。

#### B. 綁定標記者區 (核心 UX)

* **精簡列表呈現**：已加入的標記者以簡約卡片條列顯示。
* **拖曳排序 (Drag & Drop)**：每個標記者卡片左側帶有拖曳把手 (Grip)，使用者可直接拖曳調整標記顯示順序。
* **彈出視窗編輯 (Dialog)**：點擊「新增名單」或「編輯」時，以 Modal 形式覆蓋畫面操作，不干擾主畫面。
* **通訊錄搜尋帶入 (Combobox)**：在新增標記時，輸入「顯示名稱」會自動下拉搜尋通訊錄。點擊選項即可自動帶入該夥伴的「定位」與「所有社群平台帳號」。
* **動態社群平台綁定**：不綁死特定平台，使用者可自由無限新增該夥伴的社群平台（支援 X, IG, Bluesky, Threads, Plurk, Facebook 或「自訂」）。
* **無感自動儲存 (Auto-save)**：在彈出視窗按下「確認綁定」時，系統需比對通訊錄。若該「顯示名稱」不存在於通訊錄中，系統需自動將其加入通訊錄背景儲存。

#### C. 排版與 Hashtags 區

* 提供輸入框讓使用者自訂 Hashtags（如 `#FursuitFriday #Kemono`）。

#### D. 預覽與一鍵複製區 (右側浮動)

* **平台切換 Tabs**：提供各平台圖示的頁籤。點擊切換時，下方的預覽內文會**即時抽換**對應的 `@帳號`。
* **智慧 Fallback (備用) 邏輯**：若某位夥伴在選定的平台沒有填寫帳號，系統應自動尋找他填寫過的其他平台帳號，或直接顯示他的「顯示名稱」，避免出現空白。
* **同角色自動群組化**：最終輸出的文字，需將相同定位（例如「🐾 搭檔」）的夥伴合併在同一行，帳號之間以全形或半形空白分隔。（例如：`🐾 搭檔：@A帳號 @B帳號`）。
* **一鍵複製**：點擊後將預覽內容寫入剪貼簿。需提供明確的成功視覺回饋（如按鈕變綠色、文字改變），並維持 2 秒後恢復，**禁止使用原生的 `alert()**`。

---

### 3.2 通訊錄管理 (Contacts View)

作為使用者的個人夥伴資料庫，方便未來發文快速調用。

* **資料儲存**：預設儲存於瀏覽器的 `LocalStorage` 中。
* **列表展示**：以表格 (Table) 形式列出所有聯絡人，包含：顯示名稱、預設定位、已綁定之社群帳號標籤、操作按鈕。
* **新增 / 編輯功能**：
* 與發文區相同的動態社群平台欄位設計。
* 必填欄位驗證（顯示名稱不可為空），若錯誤需有行內的紅色文字提示，不使用原生 `alert()`。


* **刪除功能**：點擊刪除時，按鈕需轉變為「確認刪除 / 取消」的行內二次確認狀態，不使用原生的 `confirm()` 以確保最佳體驗。
* **舊資料自動遷移 (Migration)**：若使用者瀏覽器中存有舊版寫死平台 (twitter, ig, bsky) 的資料結構，系統進入時需自動將其轉換為新的「動態陣列 (Array)」結構。

---

## 4. 技術與細節備註 (For Developers)

1. **剪貼簿相容性**：考慮到部分環境（如預覽 iframe 或舊版瀏覽器）對 `navigator.clipboard` 的限制，建議加入 `document.execCommand('copy')` 作為備用方案，確保 100% 複製成功。
2. **狀態管理獨立性**：從「通訊錄」將資料帶入「單篇貼文」時，必須使用**深拷貝 (Deep Copy)** 處理社群平台的陣列。確保使用者在編輯單篇貼文的帳號時，不會意外改動到通訊錄的底層資料。
3. **Icon 建議**：可以使用 `lucide-vue-next` 來實作介面中的功能圖示與社群平台圖示。
4. **拖曳實作**：可使用 HTML5 原生 Drag and Drop API，或導入如 `vuedraggable` / `dnd-kit` 等輕量化套件確保跨裝置體驗。